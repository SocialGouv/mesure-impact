# Emplacement réservé aux secrets scellés par produit — pas encore utilisé.
# Tant que le chart n'est pas multitenant, `task seal ENV=dev|prod` écrit un secret
# unique et partagé dans envs/<env>/sealed-secrets/tokens.sealedsecret.yaml.
# Voir doc/architecture.md → « État de la multitenance ».
