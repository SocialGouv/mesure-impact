# Les secrets scellés du produit atterrissent ici : dev.sealedsecret.yaml, prod.sealedsecret.yaml.
# Ils ne portent que MATOMO_TOKEN_AUTH et GRIST_API_KEY ; le reste de la config est en clair
# dans produit.yaml. Générés par `PRODUIT=sante/basavi ENV=dev|prod task seal`.
