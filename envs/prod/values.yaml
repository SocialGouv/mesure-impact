environment: prod

web:
  host: mesure-impact.ovh.fabrique.social.gouv.fr
  replicas: 2

cron:
  # Aucun produit ne cible encore la prod. Repasser à false quand le premier produit
  # y sera collecté ET aura son produits/<dept>/<nom>/secrets/prod.sealedsecret.yaml :
  # ce réglage vaut pour tous les produits de l'env, pas pour un seul.
  suspend: true
  schedule: "0 6 * * *"
